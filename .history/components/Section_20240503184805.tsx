// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicSection,
  DefaultSectionProps
} from "./plasmic/daply_landing/PlasmicSection";
import { HTMLElementRefOf } from "@plasmicapp/react-web";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface SectionProps extends Omit<DefaultSectionProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultSectionProps altogether and have
// total control over the props for your component.
export interface SectionProps extends DefaultSectionProps {}

function Section_(props: SectionProps) {
  const { className, size, background } = props; // Destructure the props
  const sectionProps = { className, size, background, root: { ref } }; // Create object with expected props
  return <PlasmicSection {...sectionProps} />; // Pass expected props to PlasmicSection
}


const Section = React.forwardRef(Section_);
export default Section;
